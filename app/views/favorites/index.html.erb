<div id="contactUs">
<h1> My Favorites </h1>
<% @favorites.each do |favorite| %>
  <h3><%= favorite.apartment.address %> <button id='deletebutton'><%= link_to 'Delete', favorite, method: :delete, data: { confirm: 'Are you sure?' } %></button></h3>
  <h3><%= favorite.apartment.latitude %></h3>
  <h3><%= favorite.apartment.longitude %></h3>


<h1>My Google Map</h1>
  <style>
    #map{
      height:400px;
      width:100%;
    }
  </style>
  <div id="map"></div>
  <script>
    function initMap(){
      //Map options
      var options = {
        zoom:8,
        center:{lat:42.3601,lng:-71.0589}

      }

       // var array = <%= @favorites %>;
// console.log('@favorites')

      //New map
      var map = new
      google.maps.Map(document.getElementById('map'), options);
    // Add marker
    var marker = new google.maps.Marker({
      position:{lat:42.4668,lng:-70.9495},
      map:map
    });
// var favorites  = <%= @favorites.inspect.html_safe %>;
// for (let i = 0, i < favorites.length; i++) {
//   console.log(favorites[i])
// }

    // var infoWindow = new google.maps.InfoWindow({
    //   content: '<h1>Lynn MA</h1>'
    // });
    // marker.addListener('click', function(){
    //   infoWindow.open(map, marker);
    // });
  addMarker({lat: favorite.apartment.latitude, lng:-70.9495});
    addMarker({lat:42.4668,lng:-70.9495});
    addMarker({lat:42.8584,lng:-70.9300});
        addMarker({lat:42.7762,lng:-71.0773});
    //Add Marker function
    function addMarker(coords){
      var marker = new google.maps.Marker({
        position:coords,
        map:map
      });
    }
    }
  </script>
  <script src="https://maps.googleapis.com/maps/api/js?key=AIzaSyDboS0d_sYpMgl4MQJFw7N1Mghmi6XPU8Y&callback=initMap"
   async defer></script>

   <% end %>
